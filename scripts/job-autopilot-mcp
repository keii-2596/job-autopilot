#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PLUGIN_DIR="$(cd "${SCRIPT_DIR}/.." && pwd)"
export PYTHONPATH="${PLUGIN_DIR}/runtime${PYTHONPATH:+:${PYTHONPATH}}"
exec python3 -m job_autopilot.mcp_server
